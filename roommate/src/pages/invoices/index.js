export function renderInvoices(container) {
  container.innerHTML = `
    <div class="container-fluid">
      <h1 data-testid="page-title">Hóa đơn</h1>
      <p class="text-muted">
        Trang quản lý hóa đơn.
      </p>
    </div>
  `;
}