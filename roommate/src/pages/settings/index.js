export function renderSettings(container) {
  container.innerHTML = `
    <div class="container-fluid">
      <h1 data-testid="page-title">Cài đặt</h1>
      <p class="text-muted">
        Trang cài đặt hệ thống.
      </p>
    </div>
  `;
}