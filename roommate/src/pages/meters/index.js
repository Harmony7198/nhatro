export function renderMeters(container) {
  container.innerHTML = `
    <div class="container-fluid">
      <h1 data-testid="page-title">Chỉ số điện nước</h1>
      <p class="text-muted">
        Trang ghi chỉ số điện nước.
      </p>
    </div>
  `;
}